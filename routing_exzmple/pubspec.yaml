name: routing_example
description: A simple Flutter routing example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  auto_route_generator: ^6.6.0


flutter:
  uses-material-design: true

  dependencies:
  flutter:
    sdk: flutter
  auto_route: ^8.0.0

